// request address config
export const BASE_URL = 'http://localhost';
export const PORT = '3200';

export const REQUEST_BASE_URL = `${BASE_URL}:${PORT}`;

// graphql
export const GRAPHQL_ADDRESS = `${REQUEST_BASE_URL}/graphql`;
